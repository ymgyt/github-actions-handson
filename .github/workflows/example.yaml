name: paths-ignore-example
on:
  push:
    paths-ignore:
      - 'docs/**'
jobs:
  hello:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v2
      - uses: davidB/rust-cargo-make@v1
      - name: Run hello
        run: makers my_task
